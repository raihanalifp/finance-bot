<x-layouts.dashboard title="Telegram Users" eyebrow="CRUD">
<section class="rounded-[2rem] border border-white/70 bg-white/75 p-5 dark:border-white/10 dark:bg-white/[0.04]">@include('dashboard.partials.flash')<div class="flex items-center justify-between"><div><h2 class="text-lg font-black">Telegram Users</h2><p class="text-sm text-slate-500">Manage authorized Telegram identities.</p></div><a href="{{ route('dashboard.telegram-users.create') }}" class="rounded-2xl bg-slate-950 px-4 py-2 text-sm font-bold text-white dark:bg-white dark:text-slate-950">New user</a></div><div class="mt-5 divide-y divide-slate-100 dark:divide-white/10">@forelse($telegramUsers as $telegramUser)<div class="grid gap-3 py-4 lg:grid-cols-[1fr_auto_auto] lg:items-center"><div><p class="font-black">{{ $telegramUser->username ? '@'.$telegramUser->username : $telegramUser->telegram_chat_id }}</p><p class="text-sm text-slate-500">Chat: {{ $telegramUser->telegram_chat_id }} · {{ $telegramUser->is_authorized ? 'Authorized' : 'Blocked' }}</p></div><span class="text-sm text-slate-500">{{ optional($telegramUser->last_interaction_at)->diffForHumans() ?? 'Never' }}</span><x-dashboard.partials.actions :show="route('dashboard.telegram-users.show', $telegramUser)" :edit="route('dashboard.telegram-users.edit', $telegramUser)" :destroy="route('dashboard.telegram-users.destroy', $telegramUser)" /></div>@empty<p class="py-8 text-center text-sm text-slate-500">No Telegram users yet.</p>@endforelse</div><div class="mt-5">{{ $telegramUsers->links() }}</div></section>
</x-layouts.dashboard>
