<x-layouts.dashboard title="Settings" eyebrow="CRUD"><section class="rounded-[2rem] border border-white/70 bg-white/75 p-5 dark:border-white/10 dark:bg-white/[0.04]">@include('dashboard.partials.flash')<div class="flex items-center justify-between"><div><h2 class="text-lg font-black">Settings</h2><p class="text-sm text-slate-500">Manage key-value application settings.</p></div><a href="{{ route('dashboard.settings.create') }}" class="rounded-2xl bg-slate-950 px-4 py-2 text-sm font-bold text-white dark:bg-white dark:text-slate-950">New setting</a></div><div class="mt-5 divide-y divide-slate-100 dark:divide-white/10">@forelse($settings as $setting)<div class="grid gap-3 py-4 lg:grid-cols-[1fr_auto_auto] lg:items-center"><div><p class="font-black">{{ $setting->key }}</p><p class="text-sm text-slate-500">{{ $setting->group }} · {{ $setting->type->value }}</p></div><span class="text-sm">{{ $setting->is_encrypted ? 'Encrypted' : \Illuminate\Support\Str::limit((string)$setting->value, 50) }}</span><x-dashboard.partials.actions :show="route('dashboard.settings.show', $setting)" :edit="route('dashboard.settings.edit', $setting)" :destroy="route('dashboard.settings.destroy', $setting)" /></div>@empty<p class="py-8 text-center text-sm text-slate-500">No settings yet.</p>@endforelse</div><div class="mt-5">{{ $settings->links() }}</div></section></x-layouts.dashboard>
