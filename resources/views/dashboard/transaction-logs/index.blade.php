<x-layouts.dashboard title="Transaction Logs" eyebrow="CRUD"><section class="rounded-[2rem] border border-white/70 bg-white/75 p-5 dark:border-white/10 dark:bg-white/[0.04]">@include('dashboard.partials.flash')<div class="flex items-center justify-between"><div><h2 class="text-lg font-black">Transaction Logs</h2><p class="text-sm text-slate-500">Inspect Telegram and transaction processing logs.</p></div><a href="{{ route('dashboard.transaction-logs.create') }}" class="rounded-2xl bg-slate-950 px-4 py-2 text-sm font-bold text-white dark:bg-white dark:text-slate-950">New log</a></div><div class="mt-5 divide-y divide-slate-100 dark:divide-white/10">@forelse($logs as $log)<div class="grid gap-3 py-4 lg:grid-cols-[1fr_auto_auto] lg:items-center"><div><p class="font-black">{{ $log->message_text ? \Illuminate\Support\Str::limit($log->message_text, 80) : 'Log #'.$log->id }}</p><p class="text-sm text-slate-500">{{ $log->status->value }} · chat {{ $log->chat_id ?? '-' }}</p></div><span class="text-sm text-slate-500">{{ $log->created_at->format('d M Y H:i') }}</span><x-dashboard.partials.actions :show="route('dashboard.transaction-logs.show', $log)" :edit="route('dashboard.transaction-logs.edit', $log)" :destroy="route('dashboard.transaction-logs.destroy', $log)" /></div>@empty<p class="py-8 text-center text-sm text-slate-500">No logs yet.</p>@endforelse</div><div class="mt-5">{{ $logs->links() }}</div></section></x-layouts.dashboard>
